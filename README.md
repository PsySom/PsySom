# IdeaFlow 3.0 — Sovereign Neural Knowledge OS

**IdeaFlow 3.0** — это децентрализованная операционная система знаний, построенная на принципах полного суверенитета данных. В отличие от традиционных ИИ-платформ, IdeaFlow не имеет собственного бэкенда или базы данных: вся ваша "цифровая память" хранится исключительно в вашем личном **Google Drive**.

## 🧠 Философия проекта: Sovereign AI

1.  **Суверенитет данных:** Ваши идеи, черновики и диалоги принадлежат только вам. Приложение взаимодействует с Google Drive напрямую из браузера.
2.  **Прозрачность контекста:** ИИ имеет доступ к файловой структуре проекта, позволяя ему "видеть" ваши документы и помогать в их редактировании.
3.  **Бесшовный поток (Flow):** Мгновенное переключение между глубоким текстовым рассуждением и живым голосовым общением.

---

## 🏗 Архитектура и Логика

### 📂 Системная память на базе Drive
IdeaFlow преобразует иерархию папок Google Drive в структурированный граф знаний:
*   **`project.config.json`**: Манифест проекта, содержащий инструкции для нейронной личности (Persona), список навыков и привязанные папки.
*   **`branch_*.json`**: Изолированные ветки обсуждений, сохраняющие историю сообщений и контекст рассуждений.

### 🤖 Агентурные возможности (Tool Calling)
Используя модель **Gemini 3 Pro**, система выступает в роли автономного агента. Благодаря интеграции `driveTools`, ИИ может:
*   Самостоятельно сканировать содержимое папок.
*   Читать содержимое текстовых документов и анализировать изображения.
*   Создавать новые файлы и обновлять существующие на основе диалога.

### 🎙 Голосовой мост (Voice Link)
Интеграция с **Gemini Multimodal Live API** обеспечивает сверхнизкую задержку (low-latency) при общении. Аудиопоток (PCM 16kHz) транслируется через WebSockets, а после завершения сессии транскрипт автоматически синхронизируется с текущей веткой проекта на Диске.

### 📊 Схема потоков данных (Mermaid)

```mermaid
graph TD
    User((Пользователь)) <--> UI[React Interface]
    
    subgraph Browser_Runtime [Browser Runtime]
        UI <--> useChat[useChat Hook]
        UI <--> useProjects[Project Context]
        useChat <--> DriveSvc[Drive Service]
        useChat <--> GeminiSDK[@google/genai]
    end

    subgraph Google_Cloud [Google Cloud Services]
        DriveSvc <--> GDriveAPI[(Google Drive V3)]
        GeminiSDK <--> GeminiPro[Gemini 3 Pro / Reasoning]
        GeminiSDK <--> GeminiLive[Gemini Live API / WebSockets]
        GIS[Google Identity] --> DriveSvc
    end

    GeminiLive -- Transcription --> useChat
    GeminiPro -- Tool Calls --> DriveSvc
    DriveSvc -- File I/O --> GDriveAPI
```

---

## ✨ Ключевой функционал

*   **Neural Threads (Ветвление):** Создавайте неограниченное количество контекстных веток для разных задач внутри одного проекта.
*   **Deep Thinking:** Использование `thinkingBudget` (до 32к токенов) для решения сложных архитектурных и логических задач.
*   **Multimodal Ingestion:** Загрузка файлов (Paperclip) напрямую в "нейронное хранилище" проекта для последующего анализа ИИ.
*   **Cyberpunk UI:** Адаптивный интерфейс с поддержкой тем, горячих клавиш (V — голос, S — сохранение, T — тема) и динамических акцентов.

---

## 🛠 Технический стек

*   **Frontend:** React 19 (Vite), TypeScript, Tailwind CSS.
*   **AI:** `@google/genai` (Gemini 3 Pro Preview, Gemini 2.5 Flash Native Audio).
*   **Storage & Auth:** Google Drive API v3, Google Identity Services (OAuth 2.0).
*   **Formatting:** React Markdown, Remark GFM (для рендеринга таблиц и кода).

---

## 🚀 Быстрый старт

### 1. Предварительные требования
*   **Google Cloud Project** с включенными Drive API.
*   **OAuth 2.0 Client ID** (тип: Web Application) с разрешенным `http://localhost:5173`.
*   **API Key** от Google AI Studio (Gemini).

### 2. Настройка окружения
Создайте файл `.env` в корне проекта:
```env
VITE_GOOGLE_CLIENT_ID=ваш_client_id
VITE_GOOGLE_API_KEY=ваш_google_api_key
VITE_GEMINI_API_KEY=ваш_gemini_api_key
```

### 3. Установка
```bash
npm install
npm run dev
```

---

## 📋 Сценарии использования

### Сценарий A: Технический архитектор
1.  Создайте проект "System Core".
2.  Используйте Voice Link для диктовки требований к API.
3.  ИИ автоматически создаст файл `spec.md` в папке проекта на вашем Диске.
4.  Переключитесь на ветку "Refactoring", чтобы обсудить изменения, не затрагивая основной лог.

### Сценарий B: Исследователь данных
1.  Загрузите набор JSON-файлов через интерфейс.
2.  Попросите ИИ: "Проанализируй структуру файлов в этом секторе и предложи схему БД".
3.  ИИ прочитает файлы напрямую из хранилища и выдаст результат в Markdown.

---
*IdeaFlow 3.0 — Ваша мысль, ваш Диск, ваш интеллект.*
